<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CONTACTFLOW | Address Book</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-primary: #f8fafc;
            --bg-card: #ffffff;
            --bg-dark: #0f172a;
            --bg-sidebar: #1e293b;
            --text-primary: #0f172a;
            --text-secondary: #64748b;
            --text-muted: #94a3b4;
            --text-light: #cbd5e1;
            --accent-emerald: #10b981;
            --accent-emerald-light: #d1fae5;
            --accent-violet: #8b5cf6;
            --accent-violet-light: #ede9fe;
            --accent-rose: #f43f5e;
            --accent-rose-light: #ffe4e6;
            --accent-amber: #f59e0b;
            --accent-amber-light: #fef3c7;
            --accent-blue: #3b82f6;
            --accent-blue-light: #dbeafe;
            --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
            --shadow-md: 0 4px 12px rgba(0,0,0,0.08);
            --shadow-lg: 0 12px 40px rgba(0,0,0,0.12);
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 20px;
            --radius-xl: 28px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
        }

        /* Sidebar */
        .sidebar {
            width: 340px;
            background: var(--bg-sidebar);
            color: white;
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
            z-index: 10;
        }

        .sidebar-header {
            padding: 1.75rem;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 1.5rem;
        }

        .logo-icon {
            width: 44px;
            height: 44px;
            background: linear-gradient(135deg, var(--accent-emerald), var(--accent-violet));
            border-radius: var(--radius-md);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo-text {
            font-size: 1.4rem;
            font-weight: 700;
        }

        /* Search Box */
        .search-box {
            position: relative;
        }

        .search-input {
            width: 100%;
            padding: 0.875rem 1rem 0.875rem 2.75rem;
            background: rgba(255,255,255,0.1);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: var(--radius-md);
            color: white;
            font-family: inherit;
            font-size: 0.9rem;
            transition: all 0.3s;
        }

        .search-input::placeholder {
            color: rgba(255,255,255,0.5);
        }

        .search-input:focus {
            outline: none;
            border-color: var(--accent-emerald);
            background: rgba(255,255,255,0.15);
        }

        .search-icon {
            position: absolute;
            left: 0.875rem;
            top: 50%;
            transform: translateY(-50%);
            color: rgba(255,255,255,0.5);
        }

        /* Quick Stats */
        .quick-stats {
            padding: 1.5rem 1.75rem;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 0.75rem;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .stat-item {
            text-align: center;
            padding: 0.75rem;
            background: rgba(255,255,255,0.05);
            border-radius: var(--radius-sm);
        }

        .stat-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--accent-emerald);
        }

        .stat-label {
            font-size: 0.65rem;
            color: rgba(255,255,255,0.6);
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        /* Alphabet Index */
        .alpha-index {
            padding: 1rem 1.75rem;
            display: flex;
            flex-wrap: wrap;
            gap: 0.35rem;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .alpha-btn {
            width: 28px;
            height: 28px;
            background: transparent;
            border: none;
            border-radius: var(--radius-sm);
            color: rgba(255,255,255,0.5);
            font-size: 0.75rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }

        .alpha-btn:hover {
            background: rgba(255,255,255,0.1);
            color: white;
        }

        .alpha-btn.active {
            background: var(--accent-emerald);
            color: white;
        }

        /* Contact List */
        .contact-list {
            flex: 1;
            overflow-y: auto;
            padding: 1rem 1.75rem;
        }

        .contact-group {
            margin-bottom: 1.5rem;
        }

        .group-letter {
            font-size: 0.75rem;
            font-weight: 700;
            color: var(--accent-emerald);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 0.5rem;
            padding-left: 0.5rem;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 0.875rem;
            padding: 0.875rem;
            background: rgba(255,255,255,0.05);
            border-radius: var(--radius-md);
            margin-bottom: 0.5rem;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
        }

        .contact-item:hover {
            background: rgba(255,255,255,0.1);
            transform: translateX(4px);
        }

        .contact-item.selected {
            background: rgba(16, 185, 129, 0.2);
            border: 1px solid rgba(16, 185, 129, 0.3);
        }

        .contact-item.highlighted {
            background: var(--accent-amber-light);
            animation: highlightPulse 1s ease;
        }

        @keyframes highlightPulse {
            0%, 100% { background: var(--accent-amber-light); }
            50% { background: var(--accent-emerald-light); }
        }

        .contact-avatar {
            width: 44px;
            height: 44px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 1rem;
            flex-shrink: 0;
        }

        .contact-info {
            flex: 1;
            min-width: 0;
        }

        .contact-name {
            font-weight: 600;
            font-size: 0.95rem;
            margin-bottom: 0.15rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .contact-phone {
            font-size: 0.8rem;
            color: rgba(255,255,255,0.6);
        }

        .contact-actions {
            display: flex;
            gap: 0.25rem;
            opacity: 0;
            transition: opacity 0.2s;
        }

        .contact-item:hover .contact-actions {
            opacity: 1;
        }

        .contact-action-btn {
            width: 32px;
            height: 32px;
            background: transparent;
            border: none;
            border-radius: var(--radius-sm);
            color: rgba(255,255,255,0.6);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }

        .contact-action-btn:hover {
            background: rgba(255,255,255,0.1);
            color: white;
        }

        .contact-action-btn.delete:hover {
            background: rgba(244, 63, 94, 0.2);
            color: var(--accent-rose);
        }

        /* Add Contact Button */
        .add-contact-btn {
            margin: 1rem 1.75rem;
            padding: 1rem;
            background: var(--accent-emerald);
            border: none;
            border-radius: var(--radius-md);
            color: white;
            font-family: inherit;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            transition: all 0.3s;
        }

        .add-contact-btn:hover {
            background: #059669;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.3);
        }

        /* Main Content */
        .main {
            flex: 1;
            margin-left: 340px;
            padding: 2rem 3rem;
            min-height: 100vh;
        }

        /* Profile Card */
        .profile-card {
            max-width: 600px;
            margin: 0 auto;
            background: var(--bg-card);
            border-radius: var(--radius-xl);
            box-shadow: var(--shadow-lg);
            overflow: hidden;
            display: none;
        }

        .profile-card.active {
            display: block;
            animation: slideUp 0.4s ease;
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .profile-header {
            background: linear-gradient(135deg, var(--bg-sidebar) 0%, #334155 100%);
            padding: 2.5rem;
            text-align: center;
            position: relative;
        }

        .profile-close {
            position: absolute;
            top: 1rem;
            right: 1rem;
            width: 36px;
            height: 36px;
            background: rgba(255,255,255,0.1);
            border: none;
            border-radius: 50%;
            color: white;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background 0.2s;
        }

        .profile-close:hover {
            background: rgba(255,255,255,0.2);
        }

        .profile-avatar {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            margin: 0 auto 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            font-weight: 700;
            border: 4px solid white;
            box-shadow: var(--shadow-lg);
        }

        .profile-name {
            font-size: 1.5rem;
            font-weight: 700;
            color: white;
            margin-bottom: 0.25rem;
        }

        .profile-label {
            font-size: 0.8rem;
            color: var(--accent-emerald);
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        .profile-body {
            padding: 2rem;
        }

        .profile-info-row {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            background: var(--bg-primary);
            border-radius: var(--radius-md);
            margin-bottom: 0.75rem;
        }

        .profile-info-icon {
            width: 40px;
            height: 40px;
            background: white;
            border-radius: var(--radius-sm);
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }

        .profile-info-content {
            flex: 1;
        }

        .profile-info-label {
            font-size: 0.7rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .profile-info-value {
            font-size: 0.95rem;
            font-weight: 500;
        }

        .profile-actions {
            display: flex;
            gap: 0.75rem;
            margin-top: 1.5rem;
        }

        .profile-action-btn {
            flex: 1;
            padding: 0.875rem;
            border: none;
            border-radius: var(--radius-md);
            font-family: inherit;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            transition: all 0.3s;
        }

        .profile-action-btn.primary {
            background: var(--accent-emerald);
            color: white;
        }

        .profile-action-btn.primary:hover {
            background: #059669;
        }

        .profile-action-btn.secondary {
            background: var(--bg-primary);
            color: var(--text-primary);
        }

        .profile-action-btn.secondary:hover {
            background: var(--text-light);
        }

        .profile-action-btn.delete {
            background: var(--accent-rose-light);
            color: var(--accent-rose);
        }

        .profile-action-btn.delete:hover {
            background: var(--accent-rose);
            color: white;
        }

        /* Search Result Banner */
        .search-result-banner {
            background: var(--accent-amber-light);
            border-radius: var(--radius-lg);
            padding: 1.5rem;
            margin-bottom: 2rem;
            display: none;
            align-items: center;
            gap: 1rem;
        }

        .search-result-banner.active {
            display: flex;
            animation: slideUp 0.3s ease;
        }

        .search-result-icon {
            width: 50px;
            height: 50px;
            background: var(--accent-amber);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
        }

        .search-result-info h3 {
            font-size: 1rem;
            margin-bottom: 0.25rem;
        }

        .search-result-info p {
            font-size: 0.85rem;
            color: var(--text-secondary);
        }

        .search-result-clear {
            margin-left: auto;
            padding: 0.5rem 1rem;
            background: var(--accent-amber);
            border: none;
            border-radius: var(--radius-sm);
            color: white;
            font-family: inherit;
            font-size: 0.85rem;
            font-weight: 600;
            cursor: pointer;
        }

        /* Binary Search Visualizer */
        .search-visualizer {
            background: var(--bg-card);
            border-radius: var(--radius-lg);
            padding: 1.5rem;
            margin-bottom: 2rem;
            box-shadow: var(--shadow-md);
            display: none;
        }

        .search-visualizer.active {
            display: block;
        }

        .visualizer-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
        }

        .visualizer-title {
            font-size: 0.9rem;
            font-weight: 600;
            color: var(--accent-violet);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .visualizer-steps {
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.75rem;
            color: var(--text-secondary);
            background: var(--bg-primary);
            padding: 0.25rem 0.5rem;
            border-radius: var(--radius-sm);
        }

        .binary-steps {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .binary-step {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem;
            background: var(--bg-primary);
            border-radius: var(--radius-sm);
            font-size: 0.85rem;
        }

        .step-num {
            width: 24px;
            height: 24px;
            background: var(--accent-violet);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.7rem;
            font-weight: 700;
            flex-shrink: 0;
        }

        .step-text {
            color: var(--text-secondary);
        }

        .step-text .highlight {
            color: var(--accent-emerald);
            font-weight: 600;
        }

        .step-result {
            color: var(--accent-emerald);
            font-weight: 600;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            display: none;
        }

        .empty-state.active {
            display: block;
        }

        .empty-icon {
            width: 100px;
            height: 100px;
            background: var(--bg-card);
            border-radius: 50%;
            margin: 0 auto 1.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            box-shadow: var(--shadow-md);
        }

        .empty-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .empty-text {
            color: var(--text-secondary);
        }

        /* Modal */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.5);
            backdrop-filter: blur(4px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s;
        }

        .modal-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .modal {
            background: var(--bg-card);
            border-radius: var(--radius-xl);
            padding: 2rem;
            width: 90%;
            max-width: 450px;
            transform: scale(0.9);
            transition: transform 0.3s;
        }

        .modal-overlay.active .modal {
            transform: scale(1);
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .modal-title {
            font-size: 1.25rem;
            font-weight: 700;
        }

        .modal-close {
            width: 36px;
            height: 36px;
            background: var(--bg-primary);
            border: none;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background 0.2s;
        }

        .modal-close:hover {
            background: var(--text-light);
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .form-label {
            display: block;
            font-size: 0.8rem;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 0.4rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .form-input {
            width: 100%;
            padding: 0.875rem 1rem;
            border: 2px solid var(--bg-primary);
            border-radius: var(--radius-md);
            font-family: inherit;
            font-size: 0.95rem;
            transition: border-color 0.3s;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--accent-emerald);
        }

        .modal-actions {
            display: flex;
            gap: 0.75rem;
            margin-top: 1.5rem;
        }

        .modal-btn {
            flex: 1;
            padding: 0.875rem;
            border: none;
            border-radius: var(--radius-md);
            font-family: inherit;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
        }

        .modal-btn.primary {
            background: var(--accent-emerald);
            color: white;
        }

        .modal-btn.primary:hover {
            background: #059669;
        }

        .modal-btn.secondary {
            background: var(--bg-primary);
            color: var(--text-primary);
        }

        /* Toast */
        .toast {
            position: fixed;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%) translateY(100px);
            background: var(--bg-dark);
            color: white;
            padding: 1rem 2rem;
            border-radius: var(--radius-lg);
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            box-shadow: var(--shadow-lg);
            opacity: 0;
            transition: all 0.4s ease;
            z-index: 200;
        }

        .toast.show {
            transform: translateX(-50%) translateY(0);
            opacity: 1;
        }

        /* Responsive */
        @media (max-width: 900px) {
            .sidebar {
                width: 280px;
            }
            .main {
                margin-left: 280px;
            }
        }

        @media (max-width: 700px) {
            .sidebar {
                display: none;
            }
            .main {
                margin-left: 0;
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <div class="logo">
                <div class="logo-icon">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
                        <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                        <circle cx="9" cy="7" r="4"/>
                        <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
                        <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                    </svg>
                </div>
                <span class="logo-text">CONTACTFLOW</span>
            </div>
            <div class="search-box">
                <span class="search-icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <circle cx="11" cy="11" r="8"/>
                        <path d="m21 21-4.35-4.35"/>
                    </svg>
                </span>
                <input type="text" class="search-input" id="searchInput" placeholder="Search contacts..." oninput="handleSearch()">
            </div>
        </div>

        <div class="quick-stats">
            <div class="stat-item">
                <div class="stat-value" id="totalCount">0</div>
                <div class="stat-label">Total</div>
            </div>
            <div class="stat-item">
                <div class="stat-value" id="sortedCount">0</div>
                <div class="stat-label">Sorted</div>
            </div>
            <div class="stat-item">
                <div class="stat-value" id="searchCount">0</div>
                <div class="stat-label">Searches</div>
            </div>
        </div>

        <div class="alpha-index" id="alphaIndex">
            <!-- Generated by JS -->
        </div>

        <div class="contact-list" id="contactList">
            <!-- Contacts rendered here -->
        </div>

        <button class="add-contact-btn" onclick="openModal()">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="12" y1="5" x2="12" y2="19"/>
                <line x1="5" y1="12" x2="19" y2="12"/>
            </svg>
            Add New Contact
        </button>
    </aside>

    <!-- Main Content -->
    <main class="main" id="mainContent">
        
        <!-- Search Result Banner -->
        <div class="search-result-banner" id="searchBanner">
            <div class="search-result-icon">🔍</div>
            <div class="search-result-info">
                <h3>Binary Search Complete</h3>
                <p id="searchResultText">Found contact in X steps</p>
            </div>
            <button class="search-result-clear" onclick="clearSearch()">Clear Search</button>
        </div>

        <!-- Binary Search Visualizer -->
        <div class="search-visualizer" id="searchVisualizer">
            <div class="visualizer-header">
                <div class="visualizer-title">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <circle cx="11" cy="11" r="8"/>
                        <path d="m21 21-4.35-4.35"/>
                    </svg>
                    Binary Search Steps
                </div>
                <span class="visualizer-steps" id="visualizerSteps">O(log n)</span>
            </div>
            <div class="binary-steps" id="binarySteps">
                <!-- Steps rendered here -->
            </div>
        </div>

        <!-- Profile Card -->
        <div class="profile-card" id="profileCard">
            <div class="profile-header">
                <button class="profile-close" onclick="closeProfile()">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <line x1="18" y1="6" x2="6" y2="18"/>
                        <line x1="6" y1="6" x2="18" y2="18"/>
                    </svg>
                </button>
                <div class="profile-avatar" id="profileAvatar">JD</div>
                <h2 class="profile-name" id="profileName">John Doe</h2>
                <span class="profile-label" id="profileLabel">Software Engineer</span>
            </div>
            <div class="profile-body">
                <div class="profile-info-row">
                    <div class="profile-info-icon">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--accent-emerald)" stroke-width="2">
                            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                        </svg>
                    </div>
                    <div class="profile-info-content">
                        <div class="profile-info-label">Phone</div>
                        <div class="profile-info-value" id="profilePhone">+1 (555) 123-4567</div>
                    </div>
                </div>
                <div class="profile-info-row">
                    <div class="profile-info-icon">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--accent-violet)" stroke-width="2">
                            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                            <polyline points="22,6 12,13 2,6"/>
                        </svg>
                    </div>
                    <div class="profile-info-content">
                        <div class="profile-info-label">Email</div>
                        <div class="profile-info-value" id="profileEmail">john.doe@email.com</div>
                    </div>
                </div>
                <div class="profile-info-row">
                    <div class="profile-info-icon">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--accent-amber)" stroke-width="2">
                            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                            <circle cx="12" cy="10" r="3"/>
                        </svg>
                    </div>
                    <div class="profile-info-content">
                        <div class="profile-info-label">Location</div>
                        <div class="profile-info-value" id="profileLocation">San Francisco, CA</div>
                    </div>
                </div>
                <div class="profile-actions">
                    <button class="profile-action-btn secondary" onclick="closeProfile()">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M19 12H5M12 19l-7-7 7-7"/>
                        </svg>
                        Back
                    </button>
                    <button class="profile-action-btn delete" onclick="deleteContact()">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M3 6h18M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>
                        </svg>
                        Delete
                    </button>
                </div>
            </div>
        </div>

        <!-- Empty State -->
        <div class="empty-state" id="emptyState">
            <div class="empty-icon">📇</div>
            <h3 class="empty-title">No Contacts Yet</h3>
            <p class="empty-text">Add your first contact to get started</p>
        </div>
    </main>

    <!-- Add Contact Modal -->
    <div class="modal-overlay" id="modalOverlay">
        <div class="modal">
            <div class="modal-header">
                <h2 class="modal-title" id="modalTitle">Add New Contact</h2>
                <button class="modal-close" onclick="closeModal()">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <line x1="18" y1="6" x2="6" y2="18"/>
                        <line x1="6" y1="6" x2="18" y2="18"/>
                    </svg>
                </button>
            </div>
            <form id="contactForm" onsubmit="saveContact(event)">
                <div class="form-group">
                    <label class="form-label">Full Name</label>
                    <input type="text" class="form-input" id="inputName" placeholder="John Doe" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Phone Number</label>
                    <input type="tel" class="form-input" id="inputPhone" placeholder="+1 (555) 123-4567" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Email</label>
                    <input type="email" class="form-input" id="inputEmail" placeholder="john@email.com">
                </div>
                <div class="form-group">
                    <label class="form-label">Location</label>
                    <input type="text" class="form-input" id="inputLocation" placeholder="San Francisco, CA">
                </div>
                <div class="form-group">
                    <label class="form-label">Job Title</label>
                    <input type="text" class="form-input" id="inputJob" placeholder="Software Engineer">
                </div>
                <div class="modal-actions">
                    <button type="button" class="modal-btn secondary" onclick="closeModal()">Cancel</button>
                    <button type="submit" class="modal-btn primary">Save Contact</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Toast -->
    <div class="toast" id="toast">
        <span>✓</span>
        <span id="toastMsg">Contact saved!</span>
    </div>

    <script>
        // Avatar colors
        const avatarColors = [
            ['#f43f5e', '#fb7185'],  // Rose
            ['#f59e0b', '#fbbf24'],  // Amber
            ['#10b981', '#34d399'],  // Emerald
            ['#3b82f6', '#60a5fa'],  // Blue
            ['#8b5cf6', '#a78bfa'],  // Violet
            ['#ec4899', '#f472b6'],  // Pink
            ['#06b6d4', '#22d3ee'],  // Cyan
            ['#84cc16', '#a3e635'],  // Lime
        ];

        // Sample contacts
        let contacts = [
            { id: 1, name: 'Alice Johnson', phone: '+1 (555) 234-5678', email: 'alice@techcorp.com', location: 'New York, NY', job: 'Product Manager' },
            { id: 2, name: 'Bob Smith', phone: '+1 (555) 345-6789', email: 'bob@designstudio.com', location: 'Los Angeles, CA', job: 'UX Designer' },
            { id: 3, name: 'Carol Williams', phone: '+1 (555) 456-7890', email: 'carol@startup.io', location: 'Austin, TX', job: 'CEO' },
            { id: 4, name: 'David Brown', phone: '+1 (555) 567-8901', email: 'david@devhouse.com', location: 'Seattle, WA', job: 'Full Stack Developer' },
            { id: 5, name: 'Emma Davis', phone: '+1 (555) 678-9012', email: 'emma@analytics.ai', location: 'Boston, MA', job: 'Data Scientist' },
            { id: 6, name: 'Frank Miller', phone: '+1 (555) 789-0123', email: 'frank@cloudservices.com', location: 'Denver, CO', job: 'DevOps Engineer' },
            { id: 7, name: 'Grace Lee', phone: '+1 (555) 890-1234', email: 'grace@mobile.tech', location: 'Miami, FL', job: 'Mobile Developer' },
            { id: 8, name: 'Henry Wilson', phone: '+1 (555) 901-2345', email: 'henry@security.net', location: 'Chicago, IL', job: 'Security Analyst' },
            { id: 9, name: 'Ivy Chen', phone: '+1 (555) 012-3456', email: 'ivy@research.org', location: 'San Diego, CA', job: 'Researcher' },
            { id: 10, name: 'Jack Taylor', phone: '+1 (555) 123-4567', email: 'jack@creative.design', location: 'Portland, OR', job: 'Art Director' }
        ];

        let searchCount = 0;
        let selectedContact = null;
        let editingId = null;

        // Get initials
        function getInitials(name) {
            return name.split(' ').map(n => n[0]).join('').toUpperCase().substring(0, 2);
        }

        // Get avatar color
        function getAvatarColor(name) {
            const index = name.charCodeAt(0) % avatarColors.length;
            return `background: linear-gradient(135deg, ${avatarColors[index][0]}, ${avatarColors[index][1]})`;
        }

        // Sort contacts alphabetically
        function sortContacts() {
            contacts.sort((a, b) => a.name.localeCompare(b.name));
        }

        // Binary search
        function binarySearch(name) {
            sortContacts();
            const searchName = name.toLowerCase();
            let left = 0;
            let right = contacts.length - 1;
            const steps = [];

            while (left <= right) {
                const mid = Math.floor((left + right) / 2);
                const midName = contacts[mid].name.toLowerCase();

                steps.push({
                    step: steps.length + 1,
                    left: left,
                    right: right,
                    mid: mid,
                    midName: contacts[mid].name,
                    comparison: searchName.localeCompare(midName)
                });

                if (midName === searchName) {
                    steps.push({
                        step: steps.length + 1,
                        found: true,
                        index: mid,
                        name: contacts[mid].name
                    });
                    return { found: true, index: mid, steps };
                } else if (searchName < midName) {
                    right = mid - 1;
                } else {
                    left = mid + 1;
                }
            }

            steps.push({ step: steps.length + 1, found: false });
            return { found: false, index: -1, steps };
        }

        // Render alpha index
        function renderAlphaIndex() {
            const letters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');
            const usedLetters = new Set(contacts.map(c => c.name[0].toUpperCase()));

            document.getElementById('alphaIndex').innerHTML = letters.map(letter => `
                <button class="alpha-btn ${usedLetters.has(letter) ? '' : 'disabled'}" 
                        onclick="scrollToLetter('${letter}')"
                        style="${usedLetters.has(letter) ? '' : 'opacity: 0.3'}">
                    ${letter}
                </button>
            `).join('');
        }

        // Render contacts
        function renderContacts(filter = '') {
            sortContacts();
            const list = document.getElementById('contactList');
            const emptyState = document.getElementById('emptyState');

            if (contacts.length === 0) {
                list.innerHTML = '';
                emptyState.classList.add('active');
                return;
            }

            emptyState.classList.remove('active');

            // Group by first letter
            const groups = {};
            contacts.forEach(contact => {
                const letter = contact.name[0].toUpperCase();
                if (!groups[letter]) groups[letter] = [];
                groups[letter].push(contact);
            });

            list.innerHTML = Object.keys(groups).sort().map(letter => `
                <div class="contact-group" id="group-${letter}">
                    <div class="group-letter">${letter}</div>
                    ${groups[letter].map(contact => `
                        <div class="contact-item ${selectedContact?.id === contact.id ? 'selected' : ''}" 
                             onclick="selectContact(${contact.id})">
                            <div class="contact-avatar" style="${getAvatarColor(contact.name)}">
                                ${getInitials(contact.name)}
                            </div>
                            <div class="contact-info">
                                <div class="contact-name">${contact.name}</div>
                                <div class="contact-phone">${contact.phone}</div>
                            </div>
                            <div class="contact-actions">
                                <button class="contact-action-btn" onclick="event.stopPropagation(); editContact(${contact.id})">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                        <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
                                        <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
                                    </svg>
                                </button>
                                <button class="contact-action-btn delete" onclick="event.stopPropagation(); confirmDelete(${contact.id})">
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                        <path d="M3 6h18M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>
                                    </svg>
                                </button>
                            </div>
                        </div>
                    `).join('')}
                </div>
            `).join('');

            // Update stats
            document.getElementById('totalCount').textContent = contacts.length;
            document.getElementById('sortedCount').textContent = contacts.length;
            renderAlphaIndex();
        }

        // Select contact
        function selectContact(id) {
            selectedContact = contacts.find(c => c.id === id);
            renderContacts();
            showProfile(selectedContact);
        }

        // Show profile
        function showProfile(contact) {
            const card = document.getElementById('profileCard');
            card.classList.add('active');

            document.getElementById('profileAvatar').textContent = getInitials(contact.name);
            document.getElementById('profileAvatar').style.cssText = getAvatarColor(contact.name);
            document.getElementById('profileName').textContent = contact.name;
            document.getElementById('profileLabel').textContent = contact.job || 'Contact';
            document.getElementById('profilePhone').textContent = contact.phone;
            document.getElementById('profileEmail').textContent = contact.email || 'No email';
            document.getElementById('profileLocation').textContent = contact.location || 'No location';
        }

        // Close profile
        function closeProfile() {
            document.getElementById('profileCard').classList.remove('active');
            selectedContact = null;
            renderContacts();
        }

        // Handle search
        function handleSearch() {
            const query = document.getElementById('searchInput').value.trim();
            
            if (query.length === 0) {
                clearSearch();
                return;
            }

            searchCount++;
            document.getElementById('searchCount').textContent = searchCount;

            const result = binarySearch(query);
            renderSearchSteps(result.steps);

            if (result.found) {
                document.getElementById('searchResultText').textContent = 
                    `Found "${contacts[result.index].name}" in ${result.steps.length} steps!`;
                document.getElementById('searchBanner').classList.add('active');
                
                // Highlight found contact
                setTimeout(() => {
                    const contactItems = document.querySelectorAll('.contact-item');
                    if (contactItems[result.index]) {
                        contactItems[result.index].classList.add('highlighted');
                        contactItems[result.index].scrollIntoView({ behavior: 'smooth', block: 'center' });
                    }
                }, 500);
            } else {
                document.getElementById('searchResultText').textContent = 
                    `"${query}" not found (searched ${result.steps.length} steps)`;
                document.getElementById('searchBanner').classList.add('active');
            }
        }

        // Render binary search steps
        function renderSearchSteps(steps) {
            const container = document.getElementById('binarySteps');
            document.getElementById('searchVisualizer').classList.add('active');

            container.innerHTML = steps.map(step => {
                if (step.found) {
                    return `
                        <div class="binary-step">
                            <span class="step-num">${step.step}</span>
                            <span class="step-result">✓ Found: ${step.name} at index ${step.index}!</span>
                        </div>
                    `;
                }
                return `
                    <div class="binary-step">
                        <span class="step-num">${step.step}</span>
                        <span class="step-text">
                            Checking index <span class="highlight">${step.mid}</span>: "${step.midName}" 
                            → ${step.comparison === 0 ? '<span class="highlight">MATCH!</span>' : 
                                step.comparison < 0 ? 'Search left half' : 'Search right half'}
                        </span>
                    </div>
                `;
            }).join('');
        }

        // Clear search
        function clearSearch() {
            document.getElementById('searchInput').value = '';
            document.getElementById('searchBanner').classList.remove('active');
            document.getElementById('searchVisualizer').classList.remove('active');
            document.querySelectorAll('.contact-item.highlighted').forEach(el => {
                el.classList.remove('highlighted');
            });
        }

        // Scroll to letter
        function scrollToLetter(letter) {
            const group = document.getElementById(`group-${letter}`);
            if (group) {
                group.scrollIntoView({ behavior: 'smooth' });
            }
        }

        // Modal functions
        function openModal(contact = null) {
            editingId = contact ? contact.id : null;
            document.getElementById('modalTitle').textContent = contact ? 'Edit Contact' : 'Add New Contact';
            document.getElementById('inputName').value = contact ? contact.name : '';
            document.getElementById('inputPhone').value = contact ? contact.phone : '';
            document.getElementById('inputEmail').value = contact ? contact.email : '';
            document.getElementById('inputLocation').value = contact ? contact.location : '';
            document.getElementById('inputJob').value = contact ? contact.job : '';
            document.getElementById('modalOverlay').classList.add('active');
        }

        function closeModal() {
            document.getElementById('modalOverlay').classList.remove('active');
            editingId = null;
        }

        function editContact(id) {
            const contact = contacts.find(c => c.id === id);
            if (contact) {
                openModal(contact);
            }
        }

        function saveContact(event) {
            event.preventDefault();

            const name = document.getElementById('inputName').value.trim();
            const phone = document.getElementById('inputPhone').value.trim();
            const email = document.getElementById('inputEmail').value.trim();
            const location = document.getElementById('inputLocation').value.trim();
            const job = document.getElementById('inputJob').value.trim();

            if (editingId) {
                // Update existing
                const index = contacts.findIndex(c => c.id === editingId);
                if (index !== -1) {
                    contacts[index] = { ...contacts[index], name, phone, email, location, job };
                    showToast(`"${name}" updated!`);
                }
            } else {
                // Add new
                const newContact = {
                    id: Date.now(),
                    name, phone, email, location, job
                };
                contacts.push(newContact);
                showToast(`"${name}" added!`);
            }

            closeModal();
            renderContacts();
            clearSearch();
        }

        function confirmDelete(id) {
            if (confirm('Delete this contact?')) {
                deleteContact(id);
            }
        }

        function deleteContact() {
            if (!selectedContact) return;
            
            const name = selectedContact.name;
            contacts = contacts.filter(c => c.id !== selectedContact.id);
            closeProfile();
            renderContacts();
            showToast(`"${name}" deleted`);
        }

        function showToast(msg) {
            const toast = document.getElementById('toast');
            document.getElementById('toastMsg').textContent = msg;
            toast.classList.add('show');
            setTimeout(() => toast.classList.remove('show'), 2500);
        }

        // Initialize
        renderContacts();
    </script>
</body>
</html>
